import React from 'react';

const Loading = () => {
    return (
        <div className="container custom--container">
            <button className="custom--button button is-loading is-size-1"></button>
        </div>
    )
}

export default Loading;